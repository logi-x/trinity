[pytest]
# Don't look for conftest files in parent directories
norecursedirs = ..
markers =
    unit: unit tests that don't need backend
    real_cgroup_sweep: allow live cgroup orphan sweep (container CI only)
