# CSO Security Audit — Diff Mode

**Date:** 2026-06-26
**Mode:** daily (8/10 confidence gate)
**Scope:** `--diff` — branch `AndriiPasternak31/issue-1332` vs `origin/dev` (merge-base `85546c74`)
**Issue:** #1332 — distinguish a user-cancelled terminal as `cancelled`, separate from `failed`, across `agent_activities`

## Verdict: ✅ No findings at the daily confidence gate

The diff is a tightly-scoped observability change. It adds no new endpoints, no new request inputs, no auth changes, no dependency/CI/Docker changes, and no new rendering of user-controlled content. Nothing reaches the 8/10 reporting bar.

## Changed surface (non-test/doc)

| File | Change | Security relevance |
|------|--------|--------------------|
| `db/schema.py` | Comment only on `activity_state` | None |
| `models.py` | New enum `ActivityState.CANCELLED` + pure helper `activity_state_for_terminal()` | None — pure mapping over a str-enum, no I/O |
| `routers/chat.py` | Replace inline COMPLETED/FAILED ternaries with the helper; **new Path B** closes the open dispatch activity as CANCELLED on operator terminate | Modified endpoint is auth-gated (unchanged); see below |
| `services/task_execution_service.py` | Use helper at the CAS-lost and HTTP-error terminals | None — same code path, different terminal label |
| `ReplayTimeline.vue` | Render cancelled bars slate (`#94a3b8`) + `(Cancelled)` tooltip | No new sink — see XSS note |

## Verification performed

- **A01 Access control (terminate endpoint).** `POST /{name}/executions/{execution_id}/terminate` remains gated by `Depends(get_authorized_agent)` + `Depends(get_current_user)`. The diff adds no parameters and changes no authorization logic. The new Path B block runs only inside the existing `result.get("status") == "terminated"` branch, after the agent itself confirmed termination. `chat.py:2325-2330`.
- **A03 Injection.** New DB read `db.get_open_activity_id_for_execution()` is pre-existing (#1083) SQLAlchemy-core with parameterized comparisons — no string interpolation. `db/activities.py:172`. No `subprocess`/`os.system` introduced.
- **XSS (Phase 7).** The cancelled tooltip renders through Vue `{{ getBarTooltip(activity) }}` inside `<title>` (auto-escaped); `getBarColor` returns a hardcoded hex bound to `:fill`. No `v-html`/`innerHTML` in the component. The static string `"Execution terminated by user"` is server-set, not user input.
- **Secrets (Phase 2).** Diff grep for key/token/password patterns — clean.
- **Supply chain / CI / Docker (Phases 3–5).** No `requirements*.txt`, `package.json`, `.github/workflows/`, or `docker/` changes in the diff — out of scope.
- **Error-string disclosure.** The new `error="Execution terminated by user"` is a fixed, non-sensitive label; no stack traces or internal state leaked.

## Trend

Prior diff report `cso-diff-2026-06-23.md` covered a different branch (#654). No overlapping findings; nothing persisted, resolved, or regressed relative to it.

## Note (informational, not a finding)

Path B is correctly best-effort (`try/except` that only logs on failure) and mirrors `_close_stale_slot_activity`, so a close failure can't fail the terminate request — the right call. No action needed.
