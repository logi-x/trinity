# CSO Audit — `--diff` (Brain Orb Phase 4a KB-write) — 2026-07-01

**Mode**: `--diff` (daily gate, 8/10 confidence)
**Branch**: `feature/61-brain-orb-actions` → `feature/60-brain-orb-voice`
**Scope**: owner-gated KB-write actions (capture + link), trinity-enterprise#61
**Files audited**: `src/backend/routers/agent_brain_orb.py`, `src/backend/services/brain_orb_voice_service.py`, `src/backend/config.py`, `src/backend/routers/settings.py`, `docker/base-image/agent_server/routers/brain_orb.py`, `src/frontend/public/brain-orb/{orb.js,orb-trinity.css}`, `src/frontend/src/{views/AgentBrainOrb.vue,stores/sessions.js}`

## Result: 0 findings ≥ 8/10

The write surface is default-OFF and gated in depth. All five targeted concerns traced end-to-end and verified safe:

| # | Concern | Verdict | Evidence |
|---|---------|---------|----------|
| 1 | Owner-gate airtightness (incl. voice) | SAFE | `GET/POST /brain-orb/action[s]` are `OwnedAgentByName` (`agent_brain_orb.py:206,218`). Voice mint stays `AuthorizedAgentByName` but `can_write = BRAIN_ORB_WRITE_ENABLED and db.can_user_share_agent(...)` (line 163) → shared users get a read-only locked manifest AND a hard 403 at `/action`. Double-gated; the JWT lives in the parent orb page, not the voice iframe, so a shared user's relayed tool call carries the shared user's JWT → 403. |
| 2 | Action-enum boundary | SAFE | `_ALLOWED_ACTIONS = {capture, link}`; `action not in _ALLOWED_ACTIONS → 400` before any forward (line 242). `run_skill`/`capture_transcript` never reach the agent hook. Agent-server is unreachable directly (AgentAuthMiddleware #1159 HMAC token). |
| 3 | Injection into `_run_hook`/claude | SAFE | `_run_hook` uses `create_subprocess_exec` (exec form, no `shell=True`) with the body on **stdin** (`brain_orb.py:63-66,174`). No user string is interpolated into a shell command. No `claude -p` in 4a (deferred to #66). |
| 4 | `BRAIN_ORB_WRITE_ENABLED` bypass | SAFE | `_require_write_enabled()` gates both routes; `_agent_request` independently requires `BRAIN_ORB_ENABLED`; the voice `can_write` also folds in the flag; feature-flag `brain_orb_write_available = WRITE and ENABLED`. Both flags default OFF. |
| 5 | Idempotency cross-verb replay | SAFE | Key = `brain_orb_action:{action}:{client_key}` — the verb is folded in, so a reused client key can't replay another verb's snapshot (`agent_brain_orb.py:246`). |

## Other checks (clean)
- **Credential/PII exposure**: audit log records `details={"action": action}` only — never the note body (line 295). No secrets added to the diff.
- **XSS**: captured note bodies render via `marked.parse` + `DOMPurify.sanitize` (`orb.js:653`); the live-note label uses `textContent`. Capture body is the owner's own input regardless (self-only).
- **Rate limiting**: per-(user, agent, action) bucket on `/action`; in-flight idempotency claim released on 429/agent-error so a throttle doesn't wedge retries.
- **CSRF/CORS**: no new CORS surface; same-origin fetch from the first-party orb iframe with Bearer JWT (existing pattern).

## Deferred (out of scope — trinity-enterprise#66)
`run_skill` (headless `claude -p` exec), transcript capture, and transcript injection — the exec/injection risk surface — are intentionally NOT in this diff. Their threat model (tool-constrained sub-invocation, args/job-id validation, template.yaml allow-list ceiling, transcription spike) is captured in #66.
